# Future Work

This folder is reserved for exploratory extensions that should not disturb the locked diffusion-flow inference package in `src/diffusion_flow_inference/`.

Keep experiments here source-only by default. Generated outputs, checkpoints, datasets, and run logs should continue to live under ignored output locations.

